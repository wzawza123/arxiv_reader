你是一名严谨、批判性强的科研助理。请仔细阅读下面这篇论文（标题 + 摘要 + 从 PDF 抽取的正文内容；正文可能因模型上下文限制被截断），请针对该论文，**用中文**总结，“这篇论文尝试解决什么问题”以及“针对每个问题给出对应的本文的结论或方法”。

输出要求：
- 用中文 markdown bullet 列出本文针对的核心问题（一个一个分开，不要合并成一句）。

样例：
这篇论文《Vision Transformers Need Registers》尝试解决的核心问题是：**现代视觉Transformer（ViT）在特征图和注意力图中普遍存在高范数异常Token（artifacts），这些Token集中在图像中信息量低的背景区域，导致特征不平滑，进而损害密集预测任务（如分割、深度估计）和自监督对象发现方法的性能。**

针对这一核心问题，论文通过系统性分析提出了假设和解决方案。具体到各个分析层面，结论和方法如下：

1.  **问题：异常Token是什么？如何定义和检测？**
    *   **结论**：异常Token是输出范数（norm）远高于普通patch Token（论文设定阈值>150）的少数Token（约2%）。它们出现在模型中间层（约ViT-L第15层后），需经过充分训练且仅在大模型（≥ViT-L）中出现。通过线性探测发现，这些Token包含极少局部信息（位置预测、像素重建能力差），但能很好编码全局信息（图像分类准确率高）。
    *   **对应方法**：利用Token输出范数作为量化指标来检测和定位异常。

2.  **问题：异常Token为何出现？有何危害？**
    *   **结论**：模型在训练中学会识别信息冗余的patch（通常是背景），并将这些patch的Token“回收”为内部寄存器，用于存储和处理全局信息。这虽然满足了模型对全局聚合的需求，但代价是牺牲了相应patch的局部特征质量，导致特征图出现“尖峰”和不平滑，影响需要局部一致性的下游任务。
    *   **对应方法/解释**：提出“模型将冗余patch角色重定向为全局信息聚合器”的假设来解释该现象。

3.  **问题：如何根本性解决异常Token问题？**
    *   **核心方法**：在ViT的输入序列中显式添加额外的可学习Token，称为**寄存器Token（register tokens）**。这些Token不映射到任何图像位置，在模型输出时被丢弃（不用于下游任务），仅作为内部计算的专用空间。这为模型提供了专门处理全局信息的“正规渠道”，从而避免其滥用patch Token的角色。
    *   **关键设计**：寄存器Token的数目是超参数（论文发现4个效果最佳且开销极小），其添加位置在Patch Embedding之后、Transformer主干之前，实现简单。

**总结**：论文指出ViT默认架构缺乏处理全局信息的专用通道，迫使模型“挪用”背景patch，引发异常。通过引入中性的寄存器Token，为全局信息聚合提供专用空间，从而修复了特征质量，提升了密集任务与对象发现性能，且该方案简单、高效、适用于各类ViT。
---

{context}
